<Window x:Class="TreeTaxation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:h="http://helix-toolkit.org/wpf"
        xmlns:local="clr-namespace:TreeTaxation"
        mc:Ignorable="d"
        Title="MainWindow" Height="700" Width="1200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal">
                <Label Content="EPS = "/>
                <TextBox Text="{Binding Eps}" Width="130"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0 10 0 0">
                <Label Content="Min Pts = "/>
                <TextBox Text="{Binding MinPts}" Width="130"/>
            </StackPanel>

            <Button Grid.Row="3" Margin="0 10 0 0" Content="Сегментация" Command="{Binding DBSCANCommand}"/>
        </Grid>

        <h:HelixViewport3D Grid.Column="2"
            ZoomExtentsWhenLoaded="True" 
            ShowFrameRate="True" 
            ShowCoordinateSystem="True" ShowCameraInfo="True">
            <h:DefaultLights />

            <h:PointsVisual3D Points="{Binding Points, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Color="Red" Size="1">

            </h:PointsVisual3D>
        </h:HelixViewport3D>

        <h:HelixViewport3D Grid.Column="4"
            ZoomExtentsWhenLoaded="True" 
            ShowFrameRate="True" 
            ShowCoordinateSystem="True" ShowCameraInfo="True">
            <h:DefaultLights />

            <h:PointsVisual3D Points="{Binding FindPoints, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Color="Red" Size="2">

            </h:PointsVisual3D>
        </h:HelixViewport3D>
    </Grid>
</Window>
